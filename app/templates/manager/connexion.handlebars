{{#if reconnexion}}
<div class="alert alert-danger" role="alert">Vous devez vous reconnecter.</div>
<br>
{{/if}}
<div class="input-group">
	<span class="input-group-addon">Email</span>
	<input type="text" class="form-control" name="identifiant" placeholder="Entrez un email" {{#if identifiant}}value="{{identifiant}}"{{/if}}>
</div>
<br>
<div class="input-group">
	<span class="input-group-addon input-group-addon-danger">Mot de passe</span>
	<input type="password" class="form-control" name="pwd" placeholder="Entrez un mot de passe">
</div>
<br>
<div id="messages{{divId}}"></div>
